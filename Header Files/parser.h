#ifndef PARSER_H
#define PARSER_H

// Cst = Concrete Syntax Tree
// Ast = Abstract Syntax Tree

// concreteSyntaxTree constructCst(char* sourceCode);

// concreteSyntaxTree constructCstFromAst(abstractSyntaxTree sourceTree);

// abstractSyntaxTree constructAst(char* sourceCode);

// abstractSyntaxTree constructAstFromCst(concreteSyntaxTree sourceTree);

#endif // PARSER_H